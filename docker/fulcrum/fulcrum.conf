# Fulcrum configuration for regtest
bitcoind = bitcoin-core:8332
rpcuser = bitcoin
rpcpassword = bitcoin

# Electrum TCP (no TLS for local regtest)
tcp = 0.0.0.0:50001

# Data directory inside container
datadir = /data/fulcrum
